
// Type Any
// → Por padrão, o type: Any (implícito), está desativado através do (strict: true).   ! Para usar, sua declaração deve ser (explícita).
// → Evite usar!

function showMessage(msg:any) {
  return msg;
}

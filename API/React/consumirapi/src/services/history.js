import { createBrowserHistory } from 'history';

const history = createBrowserHistory();

export default history;

// A constante(history) vai carregar o método de criação de histórico de navegador, e então redirecionar conforme solicitado.

/*
Neste arquivo, configuramos todas as conexões entre a base de dados que está no servidor remoto e o Express via Sequelize.
Ou seja, toda comunicação ocorre através dessas configurações.
*/


const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'models-path': resolve(__dirname, 'src', 'models'),
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'),              // Histórico de alterações na base de dados.
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'),
};
